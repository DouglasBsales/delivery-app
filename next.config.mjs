/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["lh3.googleusercontent.com", "drive.google.com", 'drive.usercontent.google.com'],
  },
};

export default nextConfig;
